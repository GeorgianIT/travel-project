import React from 'react'
import { mRoad, mLake, mForest } from '../assets/import'

const Title = () => {
  return (
        <div id="home" className={`flex w-[100vw] snap-start bg-photoshop bg-cover h-[100vh] flex`}>
      </div>
  )
}

export default Title