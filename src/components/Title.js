import React from 'react'
import { mRoad, mLake, mForest } from '../assets/import'

const Title = () => {
  return (
    <div id="home" className={`flex w-[100vw] snap-start bg-bridge bg-cover h-[100vh] flex text-center text-white font-bold flex flex-col lg:text-[90px] text-[60px] leading-none justify-center`}>
      <p>DISCOVER</p>
      <p>THE WORLD</p>
      </div>
  )
}

export default Title